services:
  agente:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      # Persiste la base de datos fuera del contenedor
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/hybrid_asu.db
